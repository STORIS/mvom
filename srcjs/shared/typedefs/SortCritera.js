/**
 * Nested array consisting of sort field and sort direction pairs
 * @typedef {Array.<Array.<string, number>>} SortCriteria
 * @property {string} 0 - sort field
 * @property {number} 1 - sort order; 1 for ascending, -1 for descending
 * @example [['name', 1], ['clientId', -1]]
 *
 */
