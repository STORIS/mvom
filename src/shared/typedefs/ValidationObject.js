/**
 * Validation object structure
 * @typedef {Object} ValidationObject
 * @property {Function|AsyncFunction} validator - Required validation function to run
 * @property {string} message - Message to return if validation fails
 */
